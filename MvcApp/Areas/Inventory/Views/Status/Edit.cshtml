@using MvcApp.Areas.Inventory.Models
@model ItemStatusModel;
@{
    ViewData["Title"] = "Edit Status";
}
<div class="container-fluid">
    <div class="row">
        <!-- Sidebar -->
        <nav class="col-md-2 d-none d-md-block bg-light sidebar border-end vh-100 p-3">
            @await Html.PartialAsync("../Shared/SidebarNav.cshtml", "Create Status")
        </nav>

        <!-- Main content -->
        <main class="col-md-10 ms-sm-auto px-4 pt-4">
            <div class="text-center mb-4">
                <h1 class="fw-bold text-primary">@ViewData["Title"]</h1>
                <h4 class="text-muted">Modify an existing Status</h4>
            </div>

            <!-- Optional error message -->
            @if (ViewData["Error"] != null && ViewData["Error"].ToString().Length > 0)
            {
                <div class="alert alert-danger text-center" role="alert">
                    @ViewData["Error"]
                </div>
            }

            <!-- Edit form -->
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <form asp-area="Inventory" asp-controller="Status" asp-action="Insert" method="post">
                        <input type="number" asp-for="@Model.ItemStatusId" class="form-control" value="@Model.ItemStatusId" hidden />

                        <div class="mb-3">
                            <label asp-for="ItemStatusName" class="form-label">Status Name</label>
                            <input asp-for="ItemStatusName" class="form-control" />
                            <span asp-validation-for="ItemStatusName" class="text-danger"></span>
                        </div>

                        <div class="row justify-content-end mt-4">
                            <div class="col-auto">
                                <button type="submit" class="btn btn-primary">Save Changes</button>
                            </div>
                            <div class="col-auto">
                                <a asp-area="Inventory" asp-controller="Status" asp-action="Index" asp-route-showInactive="false" class="btn btn-outline-secondary">Cancel</a>
                            </div>
                        </div>
                    </form>
                </div>
            </div>

            <div class="alert alert-info text-center shadow-sm" role="alert">
                <strong>Note:</strong> Status updated will be reflected immediately in the system.
            </div>
        </main>

        <partial name="_ValidationScriptsPartial" />

    </div>
</div>